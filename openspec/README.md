# OpenSpec

本目录用于存放 FoPanel 的规格与变更记录。

## 约定

- 每一次需求/设计变更都新增一份文件到 `openspec/changes/`，按序号递增，例如 `002-xxx.md`。
- 新增变更后，同步更新 `openspec/changes/index.md`，把新文件追加到列表。
- 代码实现以 `openspec/changes/` 中最新的变更为准；若出现冲突，以最新编号为准并在新变更中说明取舍。
